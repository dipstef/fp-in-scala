package c09_parsing.exercises

/**
  * Hard: The slice combinator is still less efficient than it could be.
  *
  * For instance, many(char('a')).slice will still build up a List[Char], only to discard it. Can you think of a way of
  * modifying the Parser representation to make slicing more efficient?
  *
  */
object Exercise17 {

  // See Slice.scala

}
